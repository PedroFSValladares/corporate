import { InputType } from './input-type';

describe('InputType', () => {
  it('should create an instance', () => {
    const directive = new InputType();
    expect(directive).toBeTruthy();
  });
});
