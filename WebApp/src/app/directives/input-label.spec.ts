import { InputLabel } from './input-label';

describe('InputLabel', () => {
  it('should create an instance', () => {
    const directive = new InputLabel();
    expect(directive).toBeTruthy();
  });
});
