import { SelectName } from './select-name';

describe('SelectName', () => {
  it('should create an instance', () => {
    const directive = new SelectName();
    expect(directive).toBeTruthy();
  });
});
