import { InputName } from './input-name';

describe('InputName', () => {
  it('should create an instance', () => {
    const directive = new InputName();
    expect(directive).toBeTruthy();
  });
});
